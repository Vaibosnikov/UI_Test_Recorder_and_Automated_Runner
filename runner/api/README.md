# TestCraft Backend API

This service provides the backend API for the TestCraft project.  
It exposes endpoints for health checks, tests, and runs.

## Prerequisites

- Node.js (v18+ recommended)
- npm

## Install dependencies

From the `runner/api` folder:

```bash
npm install
