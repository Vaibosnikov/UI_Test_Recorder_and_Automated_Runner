export function logInfo(msg) {
  console.log(`[QA][INFO] ${msg}`);
}

export function logWarn(msg) {
  console.log(`[QA][WARN] ${msg}`);
}

export function logError(msg) {
  console.log(`[QA][ERROR] ${msg}`);
}
