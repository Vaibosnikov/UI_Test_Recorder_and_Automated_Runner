# TestCraft Dashboard (Frontend)

React + Vite frontend for the TestCraft project.  
This dashboard connects to the backend API and displays tests and runs.

## Prerequisites

- Node.js (v18+ recommended)
- Backend API running at `http://localhost:5000`

## Install dependencies

From `runner/web`:

```bash
npm install
