# TestCraft Dashboard UI (feature/dashboard-ui)

This is the frontend UI scaffold for TestCraft (React + Vite + Tailwind). It uses local mock data so UI components can be developed independently of the backend.

## Run locally

From repository root:
```bash
cd runner/web
npm install
npm run dev
