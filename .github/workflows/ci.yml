name: CI Pipeline

on:
  push:
    branches:
      - '**'   # Run on all branches
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium]   # 🔧 Start with chromium for faster iteration; add firefox, webkit once green

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Setup Node.js (with npm cache)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # Step 3: Install dependencies (root)
      - name: Install dependencies (root)
        run: npm ci

      # Optional diagnostics
      - name: "Diagnostic: which playwright & list folders"
        run: |
          node -v
          npm -v
          npx @playwright/test --version || true
          command -v playwright || true
          ls -la
          ls -la playwright || true

      # Step 4: Install Playwright browsers (avoid folder/CLI name collision)
      - name: Install Playwright browsers
        run: npx -p @playwright/test@latest playwright install --with-deps

      # Step 5: Install backend API deps
      - name: Install backend API deps
        working-directory: runner/api
        run: npm ci

      # Step 6: Start backend API on port 5000 & wait until reachable (IPv4)
      - name: Start backend API & wait
        run: |
          export PORT=${PORT:-5000}
          (cd runner/api && npm run dev) &
          npx --yes wait-on http://127.0.0.1:$PORT
        env:
          PORT: 5000
          # Uncomment & set if your API needs DB in CI:
          # DB_HOST: 127.0.0.1
          # DB_PORT: 5432
          # DB_USER: vaibh
          # DB_PASSWORD: ""
          # DB_NAME: testcraft_db
          CI: true

      # Step 7: Show which tests will run (discovery)
      - name: "Diagnostic: list discovered tests"
        run: npx playwright test --config=playwright/playwright.config.ts --list

      # Step 8: Run Playwright tests (per-browser)
      - name: "Run Playwright tests - ${{ matrix.browser }}"
        run: |
          npx playwright test \
            --config=playwright/playwright.config.ts \
            --project=${{ matrix.browser }} \
            --reporter=html,line
        env:
          BASE_URL: http://127.0.0.1:5000
          CI: true

      # Step 9: Collect artifacts into per-browser folders
      - name: Collect artifacts
        if: always()
        run: |
          mkdir -p artifacts/playwright-report-${{ matrix.browser }}
          mkdir -p artifacts/test-results-${{ matrix.browser }}
          if [ -d "playwright-report" ]; then
            cp -r playwright-report/* artifacts/playwright-report-${{ matrix.browser }}/ || true
          fi
          if [ -d "test-results" ]; then
            cp -r test-results/* artifacts/test-results-${{ matrix.browser }}/ || true
          fi

      # Step 10: Upload Playwright HTML Report
      - name: Upload Playwright HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.browser }}
          path: artifacts/playwright-report-${{ matrix.browser }}/
          if-no-files-found: warn
          retention-days: 7

      # Step 11: Upload traces/screenshots/videos
      - name: "Upload traces/screenshots/videos"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.browser }}
          path: artifacts/test-results-${{ matrix.browser }}/
          if-no-files-found: ignore
          retention-days: 7

  # (Optional) If you still want Pages publishing, we can wire a single 'site' artifact
  # produced in this job and download it in a publish job. For now, focus on green tests.
